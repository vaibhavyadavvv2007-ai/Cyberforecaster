# Core runtime (demo app + pipeline) — intentionally light
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyarrow>=14.0
streamlit>=1.31
PyYAML>=6.0
tqdm>=4.66
boto3>=1.28          # dataset S3 downloads only

# --- Training extras (ML pair installs when starting model work) ---
# torch>=2.1          # local CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
# captum>=0.7         # integrated-gradients attribution
# matplotlib>=3.7     # notebook plots
# jupyter>=1.0
